# 音频卡顿优化测试指南

## ⚠️ 方案调整说明

**初始方案问题**：将 opus_codec 移到 CPU0 后，触发了看门狗超时
- **原因**：CPU0 上的 AEC FFT 处理被 opus_codec 抢占，导致超时
- **堆栈**：`esp_aec3_hps16fft_process` 处理时被中断

**调整后的方案**：优化的 CPU1 单核方案 + 大缓冲区

## 已完成的优化

### 1. opus_codec 保持在 CPU1，优先级提升到 7
- **位置**: `main/audio/audio_service.cc:127-137`
- **配置**: CPU1, 优先级 7
- **效果**: 
  - 避免与 CPU0 的 AEC FFT 冲突
  - 优先级 7 = audio_output(7)，两任务轮流执行
  - 配合 taskYIELD()，确保解码速度跟上播放

### 2. 添加协作式调度（taskYIELD）
- **位置**: `main/audio/audio_service.cc:397, 423`
- **效果**: 每解码/编码一帧后让出 CPU，避免长时间占用

### 3. 增大软件缓冲区到 25 包（1.5 秒）
- **位置**: `main/audio/audio_service.h:45`
- **配置**: 25 包 = 1500ms 软件缓冲
- **效果**: 
  - 配合硬件 DMA 240ms = 1.74 秒总缓冲
  - 足够应对 CPU1 上的任务竞争

### 4. 优化 AudioOutputTask 等待策略
- **位置**: `main/audio/audio_service.cc:299-368`
- **效果**: 
  - 超时等待（100ms），避免无限阻塞
  - 队列为空时输出警告日志
  - 队列告急（< 5 包）时输出警告
  - 修复了日志格式化错误（%zu → %d）

## 测试步骤

### 步骤 1: 编译并烧录固件

```bash
cd /Users/machenyang/Desktop/xiaozhi-esp32-main
idf.py build
idf.py flash monitor
```

### 步骤 2: 观察启动日志

**期望看到**：
```
I (xxxx) AudioService: ✅ opus_codec 任务已创建：CPU1, 优先级7
I (xxxx) AudioService: ✅ audio_output 任务已创建：CPU1, 优先级7
```
（两个任务同优先级，会轮流执行）

### 步骤 3: 测试音频播放流畅性

1. **触发 TTS 播放**（让机器人说话）
2. **观察日志**，应该看到：
   ```
   I (xxxx) AudioService: ✅ 播放队列恢复，队列大小: 15
   ```
   （应该 > 10 包，说明解码速度够快）
3. **听音频**，应该：
   - ✅ 播放流畅，无"机关枪"式卡顿
   - ✅ 响应延迟低（约 1.1 秒）
   - ✅ 音质清晰

### 步骤 4: 测试看门狗超时

**观察日志**，应该：
- ✅ **不再出现** `task_wdt: Task watchdog got triggered`
- ✅ **不再出现** `Guru Meditation Error`
- ✅ 系统稳定运行

### 步骤 5: 测试眼睛动画流畅性

1. **在播放音频时观察眼睛动画**
2. **期望效果**：
   - ✅ 眼睛动画流畅，无明显卡顿
   - ✅ 眨眼、视线移动正常
   - ✅ 帧率保持稳定

### 步骤 6: 测试缓冲区充足性

**观察日志**，在播放音频时：
```
W (xxxx) AudioService: ⚠️ 播放队列告急: 4 包（可能出现卡顿）
```

- ✅ **偶尔出现**（< 5% 时间）：正常，缓冲区在工作
- ⚠️ **频繁出现**（> 20% 时间）：需要进一步调整，见故障排查

## 性能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| **音频卡顿** | 无卡顿 | 听音频，应该连续流畅 |
| **响应延迟** | < 1.8s | 从说话到开始播放的时间（增大缓冲后略高）|
| **看门狗超时** | 0 次 | 观察日志，不应出现 watchdog 错误 |
| **眼睛动画帧率** | > 15 FPS | 观察动画流畅度 |
| **播放队列告急** | < 5% 时间 | 观察警告日志频率 |

## 故障排查

### 问题 1: 仍然出现看门狗超时

**可能原因**: AFE 的 AEC 处理仍然被阻塞

**解决方案 A**: 降低 opus_codec 优先级
```cpp
// main/audio/audio_service.cc:135
6, // 改为 5（进一步降低优先级）
```

**解决方案 B**: 禁用 AEC（如果不需要回声消除）
```cpp
// main/audio/processors/afe_audio_processor.cc
afe_config->aec_init = false;  // 禁用 AEC
```

### 问题 2: 播放仍有轻微卡顿

**可能原因**: 播放队列被耗尽

**解决方案 A**: 进一步增加软件缓冲区
```cpp
// main/audio/audio_service.h:45
#define MAX_PLAYBACK_TASKS_IN_QUEUE 30 // 从 25 增加到 30（1.8秒）
```

**解决方案 B**: 提高 opus_codec 优先级（谨慎！）
```cpp
// main/audio/audio_service.cc:135
7, // 从 6 提高到 7（与 audio_output 同级）
```
⚠️ 注意：这可能会影响播放任务

### 问题 3: 眼睛动画变卡顿

**可能原因**: LVGL 任务被 opus_codec 抢占

**解决方案**: 这种情况不太可能发生，因为 opus_codec 优先级（4）低于 LVGL。
如果确实发生，可以在 LVGL 任务中也添加 `taskYIELD()`。

## 预期改善

| 指标 | 优化前 | 优化后 | 改善 |
|------|--------|--------|------|
| **音频卡顿** | 机关枪式（频繁） | 流畅播放 | ✅ 消除 |
| **响应延迟** | ~1.2s | ~1.7s | ⚠️ 略微增加（换取稳定性）|
| **眼睛动画** | 流畅 | 流畅 | ✅ 不受影响 |
| **看门狗超时** | 移到CPU0时触发 | 不再触发 | ✅ 解决 |
| **CPU利用率** | CPU1高负载 | CPU1优化调度 | ✅ 改善 |

## 核心原理总结

1. **避免跨核冲突**: 保持 opus_codec 在 CPU1，避免与 CPU0 的 AEC FFT 冲突
2. **协作式调度**: `taskYIELD()` 确保 opus_codec 不会长时间占用 CPU1
3. **优先级平衡**: opus_codec(6) < audio_output(7)，播放优先但解码及时
4. **大缓冲策略**: 1.74秒总缓冲（1500ms 软件 + 240ms 硬件）应对任务竞争

## 测试完成标准

- ✅ 音频播放流畅，无"机关枪"式卡顿
- ✅ 响应延迟可接受（< 1.8 秒）
- ✅ 无看门狗超时错误（**最关键**）
- ✅ 眼睛动画流畅（帧率 > 15 FPS）
- ✅ 播放队列告急警告少于 5% 时间

## 权衡说明

此方案优先保证：
1. **稳定性**：无看门狗超时（✅ 最重要）
2. **流畅性**：无音频卡顿
3. **延迟**：略微增加（1.2s → 1.7s）以换取稳定性

如果所有标准都满足，优化成功！🎉

