# 💾 NVS 空间管理完整指南

## 📊 NVS 空间分配

### 分区表配置

**您的设备 NVS 分区大小**：
```csv
# partitions/v2/16m.csv
nvs,        data, nvs,     0x9000,   24K,
```

**总容量**：24 KB (24,576 字节)

---

## 🗄️ 当前 NVS 存储内容

### 1. WiFi 系统数据（约 2-3 KB）

| 数据 | 命名空间 | 键名 | 大小 | 说明 |
|------|---------|------|------|------|
| WiFi 配置 | `nvs.net80211` | 多个 | ~1 KB | SSID、密码、信道等 |
| WiFi 校准数据 | `phy` | 多个 | ~1-2 KB | 射频校准参数 |

**特点**：
- ⚠️  系统自动管理，**不能删除**
- ⚠️  删除会导致 WiFi 功能异常

---

### 2. 应用配置数据（约 0.5-1 KB）

| 数据 | 命名空间 | 键名 | 大小 | 说明 |
|------|---------|------|------|------|
| 显示主题 | `settings` | `theme` | ~50 bytes | 显示主题设置 |
| OTA 版本 | `settings` | `ota_*` | ~100 bytes | OTA 升级相关 |
| 其他设置 | `settings` | 多个 | ~200 bytes | 音量、亮度等 |

**特点**：
- ✅ 可以删除（会重置为默认值）
- 🔄 删除后重新配置即可

---

### 3. 电子宠物数据（约 0.2 KB）

| 数据 | 命名空间 | 键名 | 大小 | 说明 |
|------|---------|------|------|------|
| 宠物状态 | `pet_system` | `pet_state` | ~120 bytes | 心情、饱食度、清洁度等 |
| 宠物类型 | `pet_system` | `pet_state` | 包含在上面 | 当前宠物类型 |

**特点**：
- ✅ 可以删除（宠物重置为默认状态）
- 🔄 删除后宠物数据从头开始

---

### 4. 用户画像数据（约 0.3 KB）

| 数据 | 命名空间 | 键名 | 大小 | 说明 |
|------|---------|------|------|------|
| 学习数据 | `user_profile` | `profile_data` | ~200 bytes | 互动统计、话题偏好等 |

**特点**：
- ✅ 可以删除（学习系统重新开始）
- 🔄 删除后用户画像从头积累

---

## 📈 空间占用估算

### 正常使用（约 4-5 KB）

```
WiFi 系统数据：    2-3 KB   (40-50%)
应用配置数据：    0.5-1 KB  (5-10%)
电子宠物数据：    0.2 KB    (1%)
用户画像数据：    0.3 KB    (1-2%)
NVS 元数据：      ~1 KB     (5%)
────────────────────────────
总计：            4-5 KB    (20-25%)
剩余空间：        19-20 KB  (75-80%)
```

**结论**：**空间充足！**

---

### 长期使用（约 6-8 KB）

```
WiFi 系统数据：    3-4 KB   (WiFi 重连、更新配置)
应用配置数据：    1-2 KB   (更多设置项)
电子宠物数据：    0.2 KB   (不变)
用户画像数据：    0.3 KB   (不变)
NVS 元数据：      1-2 KB   (碎片化)
────────────────────────────
总计：            6-8 KB    (30-35%)
剩余空间：        16-18 KB  (65-70%)
```

**结论**：**仍然充足！**

---

## ⚠️ NVS 空间不足的情况

### 什么时候会满？

1. **WiFi 频繁切换**（每次切换写入新配置）
2. **大量设置项**（大量字符串配置）
3. **NVS 碎片化**（多次删除写入导致碎片）
4. **Bug 导致重复写入**（某个模块频繁保存）

### 空间不足的表现

**日志**：
```
E (xxx) UserProfile: ❌ Failed to write profile data: ESP_ERR_NVS_NOT_ENOUGH_SPACE
E (xxx) Settings: ⚠️  NVS空间不足，无法保存设置
```

**行为**：
- ❌ 无法保存新设置
- ❌ 无法保存宠物状态
- ❌ 无法保存用户画像
- ✅ 但系统**不会崩溃**（已优雅处理）

---

## 🔧 解决方案

### 方案 1：擦除 NVS（推荐，简单快速）

#### 命令行擦除（全部擦除）

```bash
# 擦除整个 NVS 分区（0x9000 开始，24KB）
python -m esptool --chip esp32s3 --port /dev/tty.usbmodem101 erase_region 0x9000 0x6000
```

#### VSCode 擦除（推荐）

```
VSCode 底部状态栏:
🗑️ Erase Flash
```

**效果**：
- ✅ 释放所有 NVS 空间
- ✅ 回到出厂状态

**影响**：
- ❌ WiFi 配置丢失（需要重新配网）
- ❌ 所有设置恢复默认
- ❌ 宠物数据清空
- ❌ 用户画像清空
- ✅ 其他功能不受影响（ESP-SR 模型、固件等）

---

### 方案 2：选择性擦除（高级）

#### 只擦除应用数据（保留 WiFi）

**步骤 1**：创建脚本 `erase_app_nvs.sh`

```bash
#!/bin/bash
# 只擦除应用命名空间，保留 WiFi 配置

PORT="/dev/tty.usbmodem101"

echo "⚠️  将擦除以下 NVS 数据："
echo "  - 电子宠物数据"
echo "  - 用户画像数据"
echo "  - 应用设置"
echo ""
echo "✅ 保留以下数据："
echo "  - WiFi 配置"
echo "  - WiFi 校准数据"
echo ""
read -p "确认擦除？(y/N) " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    echo "已取消"
    exit 1
fi

# 使用 NVS 工具擦除特定命名空间
idf.py -p $PORT nvs-erase pet_system
idf.py -p $PORT nvs-erase user_profile
idf.py -p $PORT nvs-erase settings

echo "✅ 擦除完成！WiFi 配置已保留"
```

**步骤 2**：运行

```bash
bash erase_app_nvs.sh
```

**效果**：
- ✅ 释放应用数据空间
- ✅ **保留 WiFi 配置**

---

### 方案 3：代码中自动清理

#### 修改 `main.cc`，添加 NVS 空间检查

```cpp
// main.cc
static void check_nvs_space() {
    nvs_stats_t nvs_stats;
    esp_err_t err = nvs_get_stats(NULL, &nvs_stats);
    if (err != ESP_OK) {
        ESP_LOGW(TAG, "Failed to get NVS stats");
        return;
    }
    
    size_t used = nvs_stats.used_entries;
    size_t free = nvs_stats.free_entries;
    size_t total = used + free;
    uint8_t usage_percent = (used * 100) / total;
    
    ESP_LOGI(TAG, "📊 NVS usage: %d/%d entries (%d%%)", 
             used, total, usage_percent);
    
    // 如果使用超过 80%，发出警告
    if (usage_percent > 80) {
        ESP_LOGW(TAG, "⚠️  NVS 使用率超过 80%%，建议清理");
    }
    
    // 如果超过 95%，自动擦除旧数据
    if (usage_percent > 95) {
        ESP_LOGE(TAG, "🚨 NVS 空间严重不足，自动清理应用数据...");
        
        // 擦除非关键数据
        nvs_handle_t nvs;
        nvs_open("user_profile", NVS_READWRITE, &nvs);
        nvs_erase_all(nvs);
        nvs_commit(nvs);
        nvs_close(nvs);
        
        nvs_open("pet_system", NVS_READWRITE, &nvs);
        nvs_erase_all(nvs);
        nvs_commit(nvs);
        nvs_close(nvs);
        
        ESP_LOGI(TAG, "✅ 清理完成，已释放空间");
    }
}

extern "C" void app_main(void) {
    // ... NVS 初始化后
    
    check_nvs_space();  // 检查 NVS 空间
    
    // ... 启动应用
}
```

**效果**：
- ✅ 自动监控 NVS 使用率
- ✅ 超过阈值自动清理
- ✅ 保留关键数据（WiFi 配置）

---

## 🎯 擦除 NVS 的影响详解

### ✅ 不受影响的功能

| 功能 | 存储位置 | 影响 |
|------|---------|------|
| ESP-SR 模型 | `model` 分区 (Flash) | ✅ 不受影响 |
| 固件程序 | `app` 分区 (Flash) | ✅ 不受影响 |
| 资产文件 | `assets` 分区 (Flash) | ✅ 不受影响 |
| OTA 分区 | `ota_0/ota_1` (Flash) | ✅ 不受影响 |

**重要**：擦除 NVS **只影响配置数据**，不影响程序和功能！

---

### ❌ 受影响的数据

| 数据 | 影响 | 恢复方法 |
|------|------|---------|
| WiFi 配置 | ❌ 丢失 | 重新配网（音频配网/WebUI） |
| 显示主题 | ❌ 恢复默认 | 重新设置主题 |
| 音量/亮度 | ❌ 恢复默认 | 重新调整 |
| 宠物状态 | ❌ 重置 | 从头养宠物 |
| 用户画像 | ❌ 清空 | 系统重新学习 |

**恢复时间**：
- WiFi 配网：2-5 分钟
- 其他设置：1-2 分钟
- 宠物/画像：自动积累（无需手动）

---

## 🧪 测试 NVS 空间

### 查看当前使用率

**方法 1**：使用 esptool 读取

```bash
# 读取 NVS 分区
python -m esptool --chip esp32s3 --port /dev/tty.usbmodem101 read_flash 0x9000 0x6000 nvs_backup.bin

# 分析大小（简单）
ls -lh nvs_backup.bin
```

**方法 2**：在代码中打印

在 `main.cc` 的 `app_main()` 中添加：

```cpp
nvs_stats_t nvs_stats;
if (nvs_get_stats(NULL, &nvs_stats) == ESP_OK) {
    ESP_LOGI(TAG, "📊 NVS: used=%d, free=%d, total=%d", 
             nvs_stats.used_entries, 
             nvs_stats.free_entries,
             nvs_stats.used_entries + nvs_stats.free_entries);
}
```

**输出示例**：
```
I (xxx) main: 📊 NVS: used=15, free=496, total=511
                        ↑ 3%      ↑ 97%
```

---

## 🔄 最佳实践

### 1. 定期检查 NVS 使用率

**每 1000 次对话检查一次**：

```cpp
// user_profile.cc
void UserProfile::RecordInteraction(...) {
    data_.interaction_count_7d++;
    
    // 每 1000 次检查一次
    if (data_.interaction_count_7d % 1000 == 0) {
        nvs_stats_t stats;
        nvs_get_stats(NULL, &stats);
        uint8_t usage = (stats.used_entries * 100) / (stats.used_entries + stats.free_entries);
        ESP_LOGI(TAG, "📊 NVS usage: %d%%", usage);
    }
}
```

---

### 2. 设置合理的自动保存间隔

**用户画像**：
```cpp
// 默认 10 分钟保存一次（减少 NVS 写入次数）
user_profile.SetAutoSaveInterval(600);  // 秒
```

**电子宠物**：
```cpp
// 每小时保存一次（宠物状态变化不频繁）
pet_system.SetAutoSaveInterval(3600);  // 秒
```

---

### 3. 避免频繁写入同一个键

**❌ 不好的做法**：
```cpp
// 每秒写入一次（浪费 NVS 写入次数）
void OnTimer() {
    settings.SetInt("count", count++);
}
```

**✅ 好的做法**：
```cpp
// 内存中计数，定期保存
void OnTimer() {
    count_++;
}

void OnShutdown() {
    settings.SetInt("count", count_);  // 关机时才保存
}
```

---

## 📋 快速参考

### 擦除 NVS 快速命令

```bash
# 方法 1：esptool（完全擦除）
python -m esptool --chip esp32s3 --port /dev/tty.usbmodem101 erase_region 0x9000 0x6000

# 方法 2：VSCode（推荐）
VSCode 状态栏: 🗑️ Erase Flash

# 方法 3：idf.py
idf.py -p /dev/tty.usbmodem101 erase-flash
```

### 擦除后的恢复步骤

1. **WiFi 配网**（2-5 分钟）
   - 音频配网：说出 WiFi 信息
   - WebUI 配网：连接 AP，浏览器配置

2. **其他设置**（自动/手动）
   - 主题、音量、亮度：重新设置
   - 宠物、画像：自动从默认值开始

3. **验证功能**
   - 对话测试：确认 ESP-SR 正常
   - 宠物测试：确认宠物系统正常
   - 学习测试：对话几次，看到学习日志

---

## ✅ 总结

### NVS 空间充足吗？

| 场景 | 使用率 | 剩余空间 | 状态 |
|------|-------|---------|------|
| 正常使用 | 20-25% | 75-80% (18-19 KB) | ✅ 非常充足 |
| 长期使用 | 30-35% | 65-70% (15-17 KB) | ✅ 充足 |
| 极端情况 | 50-60% | 40-50% (10-12 KB) | ✅ 够用 |
| 空间不足 | >90% | <10% (<2 KB) | ⚠️  需要清理 |

**结论**：**24 KB 的 NVS 空间对于当前功能来说非常充足！**

---

### 擦除 NVS 的影响

| 影响程度 | 数据 | 恢复难度 |
|---------|------|---------|
| 🔴 高 | WiFi 配置 | 中等（需要重新配网） |
| 🟡 中 | 应用设置 | 低（重新设置即可） |
| 🟢 低 | 宠物数据 | 无（自动重新开始） |
| 🟢 低 | 用户画像 | 无（自动重新学习） |

**建议**：
- ✅ 正常情况下**无需擦除**
- ✅ 如果空间不足，**先尝试代码中的自动清理**
- ✅ 如果仍不够，**擦除 NVS 并重新配网**

---

**NVS 空间很充足，不用担心！** ✨🎉

