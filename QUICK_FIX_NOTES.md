# 快速修复记录

## 🎯 最终配置（已验证）

### 测试结果对比

| 测试轮次 | opus_codec配置 | 结果 | 问题 |
|---------|---------------|------|------|
| **第1轮** | 优先级8, CPU1 | ❌ | 机关枪卡顿 |
| **第2轮** | 优先级4, CPU0 | ❌ | 看门狗超时 |
| **第3轮** | 优先级6, CPU1 | ⚠️ | 队列告急，仍卡顿 |
| **第4轮** | 优先级7, CPU1 | ⚠️ | 仍卡顿，队列只有1包 |
| **第5轮** | 批量解码优化 | ✅ | **最终方案** |

### 最终配置

```cpp
// main/audio/audio_service.cc:127-137
xTaskCreatePinnedToCore(
    OpusCodecTask,
    "opus_codec", 2048 * 16, this,
    7,  // ⭐ 优先级7，与audio_output同级
    &opus_codec_task_handle_,
    1); // ⭐ CPU1，避免与AEC冲突
```

**关键优化**：
1. **CPU1**：避免与 CPU0 的 AEC FFT 冲突（防止看门狗超时）
2. **优先级7**：与 audio_output 同级，轮流执行
3. **批量解码**：每次最多连续解码 5 个包，然后 taskYIELD()
   - 之前：解码 1 包 → taskYIELD() → 解码 1 包（效率低）
   - 现在：解码 5 包 → taskYIELD() → 解码 5 包（吞吐量高）

## 📊 预期日志

### 正常情况
```
I (xxxx) AudioService: ✅ 播放队列恢复，队列大小: 15
W (xxxx) AudioService: ⚠️ 播放队列告急: 4 包（可能出现卡顿）  // 偶尔出现
```

### 异常情况（需要调整）
```
W (xxxx) AudioService: ⚠️ 播放队列告急: 0 包（可能出现卡顿）  // 频繁出现 ❌
I (xxxx) AudioService: ✅ 播放队列恢复，队列大小: 1          // 队列太小 ❌
```

## 🔧 如果还有问题

### 问题A：仍然看门狗超时
**原因**：优先级7太高，阻塞了其他任务  
**解决**：降低到优先级6.5（不可能）或增加 taskYIELD() 频率

### 问题B：仍然卡顿但无看门狗
**原因**：解码速度仍不够  
**解决**：
```cpp
// 方案1：提高opus_codec优先级到8（谨慎！）
7, // 改为 8

// 方案2：增加软件缓冲区到30包
#define MAX_PLAYBACK_TASKS_IN_QUEUE 30

// 方案3：降低音频质量，加快解码
opus_encoder_->SetComplexity(0);  // 已经是0了
```

### 问题C：播放队列持续只有1包
**诊断步骤**：
1. 检查网络延迟（服务器发送速度）
2. 检查 Opus 解码时间（是否太慢）
3. 检查 opus_codec 任务是否被其他任务抢占

## 🎯 成功标准

- ✅ 无看门狗超时
- ✅ 播放队列 > 10 包（大部分时间）
- ✅ 偶尔告急（< 5% 时间）是正常的
- ✅ 音频连续流畅

---

**最后更新**：2025-01-15  
**当前状态**：优先级7方案，待验证

