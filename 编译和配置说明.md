# 小智ESP32固件编译和配置说明

## 📋 您的配置信息

- **板子型号**: Palqiqi (帕奇奇机器人)
- **WiFi名称**: 2223
- **WiFi密码**: GYXZXY88
- **服务器地址**: http://192.168.1.151:8003/xiaozhi/ota/
- **Websocket地址**: ws://192.168.1.151:8000/xiaozhi/v1/

## ✅ 已完成的配置

1. ✅ 板子类型已设置为 PALQIQI
2. ✅ OTA地址已修改为您的服务器地址
3. ✅ 固件源码准备就绪

## 🚀 编译固件步骤

### 方法一：完整编译（推荐）

```bash
# 1. 进入固件目录
cd /Users/machenyang/Desktop/xiaozhi-esp32-main

# 2. 设置编译目标为ESP32-S3
idf.py set-target esp32s3

# 3. 清理之前的编译
idf.py fullclean

# 4. 编译固件
idf.py build

# 5. 连接板子后烧录（连接USB线）
idf.py -p /dev/cu.usbserial* flash monitor
```

### 方法二：使用网页烧录工具

```bash
# 1. 编译固件
cd /Users/machenyang/Desktop/xiaozhi-esp32-main
idf.py build

# 2. 打包固件
cd scripts
python release.py
```

编译完成后，固件文件在: `build/merged-binary.bin`

然后使用Chrome浏览器打开: https://espressif.github.io/esp-launchpad/
按照提示烧录 `merged-binary.bin` 文件

## 📱 WiFi配网步骤

固件烧录成功后，Palqiqi会自动进入配网模式：

### 步骤一：连接热点

1. 板子会创建一个WiFi热点，名称类似: `xiaozhi-XXXXXX`
2. 用手机或电脑连接这个热点
3. 连接成功后，会自动弹出配网页面
4. 如果没有自动弹出，手动在浏览器打开: `http://192.168.4.1`

### 步骤二：配置WiFi

在配网页面中：
1. 选择您的WiFi网络: **2223**
2. 输入WiFi密码: **GYXZXY88**
3. 点击"连接"按钮
4. 等待板子重启并连接到您的WiFi

### 步骤三：验证连接

板子连接WiFi成功后：
- 屏幕会显示连接状态
- 会自动从OTA服务器获取服务器地址
- 开始连接到您的小智服务器

## 🧪 测试方法

### 1. 查看串口日志

如果板子还连接着电脑：

```bash
idf.py monitor
```

您会看到类似日志：
```
WiFi Connected
OTA URL: http://192.168.1.151:8003/xiaozhi/ota/
Websocket: ws://192.168.1.151:8000/xiaozhi/v1/
Connected to server
```

### 2. 测试语音对话

说出唤醒词: **"你好小智"**
然后说: **"今天天气怎么样"**

## ⚠️ 常见问题

### 1. WiFi连接失败

**可能原因:**
- WiFi密码错误
- 信号太弱
- 路由器限制了设备数量

**解决方法:**
- 重启板子，重新配网
- 确认WiFi名称和密码正确
- 将板子靠近路由器

### 2. 无法连接到服务器

**可能原因:**
- 服务器没有启动
- IP地址不在同一局域网
- 防火墙阻止了连接

**解决方法:**
- 确认服务器已启动: 
  ```bash
  cd /Users/machenyang/Desktop/xiaozhi-esp32-server-main/main/xiaozhi-server
  python app.py
  ```
- 确认板子和服务器在同一WiFi网络
- 关闭防火墙或添加例外规则

### 3. 编译失败

**可能原因:**
- ESP-IDF环境未配置
- Python依赖缺失

**解决方法:**
- 确认ESP-IDF已正确安装
- 运行 `idf.py --version` 检查版本
- 重新运行 `idf.py set-target esp32s3`

## 🔧 高级配置

### 重置WiFi配置

如果需要更换WiFi，长按板子上的复位按钮5秒以上，板子会清除WiFi配置并重新进入配网模式。

### 查看板子IP地址

板子连接WiFi后，可以在路由器管理界面查看分配的IP地址，或者通过串口日志查看。

### 使用串口命令

连接串口后，可以输入以下命令：
- `wifi_info` - 查看WiFi连接信息
- `server_info` - 查看服务器连接状态
- `reset_wifi` - 重置WiFi配置

## 📚 相关文档

- [固件编译详细教程](docs/firmware-build.md)
- [固件配置教程](docs/firmware-setting.md)
- [常见问题FAQ](docs/FAQ.md)

## 🆘 需要帮助？

如果遇到问题：
1. 查看串口日志: `idf.py monitor`
2. 查看服务器日志: `tail -f main/xiaozhi-server/tmp/server.log`
3. 提交Issue: https://github.com/xinnan-tech/xiaozhi-esp32/issues

